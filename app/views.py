from django.shortcuts import render
from .figures import return_graph
from .models import FunctionModel

def home(request):
    #compositemodel = CompositeModel.objects.get(pk=1)
    #compositemodel_i = CompositeModel.objects.get(pk=2)
    #context = {'graph_1': plot_with_args(compositemodel.x, compositemodel.y)}
    #context = {'graph': return_graph(compositemodel_i.x, compositemodel_i.y)}
    #return render(request, 'dashboard.html', context)
    pass

